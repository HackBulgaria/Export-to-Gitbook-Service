{
  "name": "Gitbook-Export-Service",
  "version": "0.0.0",
  "description": "A service that exports given markdowns via POST to a Gitbook HTML",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hack Bulgaria",
  "license": "ISC",
  "dependencies": {
    "express": "^4.1.0"
  }
}
